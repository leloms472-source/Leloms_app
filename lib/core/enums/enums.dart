enum ResourceType { pdf, summary, flashcard, quiz, exam }
enum ResourceVisibility { public, private, careerOnly }
enum SessionType { pomodoro, deep, custom }
enum Difficulty { beginner, intermediate, advanced }
enum AiJobStatus { pending, processing, completed, failed }
enum HelpRequestStatus { open, inProgress, resolved }
